# Hoku Health Care - Python Dependencies
# Production-ready pins for reproducible builds.
# Day 10 additions: prometheus-client, ruff, mypy, locust, httpx pinned for
# the async load client. Everything is == pinned so CI, Docker, and Render
# resolve identical trees.

# ---- Web framework & server ----------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ---- Database -------------------------------------------------------------
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
alembic==1.13.2
pgvector==0.2.5

# ---- Config & env ---------------------------------------------------------
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ---- AI / LLM -------------------------------------------------------------
langchain==0.2.6
langchain-groq==0.1.6
groq==0.9.0
sentence-transformers==3.0.1

# ---- Auth & security ------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ---- HTTP client (also used by the async load test) -----------------------
httpx==0.27.0

# ---- Token counting (optional; Windows fallback to len/4 if unavailable) --
tiktoken==0.7.0

# ---- Day 6: fuzzy symptom-to-specialist matching --------------------------
rapidfuzz==3.9.0

# ---- Day 10: observability ------------------------------------------------
prometheus-client==0.20.0

# ---- Testing --------------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# ---- Day 10: quality gates & load testing ---------------------------------
ruff==0.5.0
mypy==1.10.1
locust==2.29.1